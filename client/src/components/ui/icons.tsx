import React from "react";
import { Check, X, Copy, Download, RefreshCw, Clipboard, Router } from "lucide-react";

export const Icons = {
  copy: Copy,
  check: Check,
  close: X,
  download: Download,
  refresh: RefreshCw,
  paste: Clipboard,
  router: Router,
};